# Linux Desktop App (Tauri 2 + React 19)
# In Tauri desktop mode, the app interacts directly with native Rust IPC commands and local SQLite/PostgreSQL.
# Optional fallback API URL if run in pure browser dev preview:
VITE_API_BASE_URL=http://localhost:3000

